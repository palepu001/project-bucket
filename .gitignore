# Dependencies — unrooted so the nested static/* packages are covered too
node_modules/

# Build output
/dist/
# Custom UI bundles are generated by each resource's build.js (esbuild bundle +
# a copied pdf.js worker). Regenerate with `npm run build` inside each
# static/* package before `forge deploy` — they are derived, not sources.
/static/panel/build/
/static/attachment-watcher/build/

# Forge
/.forge/

# Editors / OS
/.vscode/
.idea
.DS_Store
